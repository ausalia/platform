import CheckEmailNotice from "@/components/check-email-notice";

export default function CheckEmailPage() {
  return (
    <CheckEmailNotice
      title="Check your email"
      body="We sent a confirmation link. Click it, then sign in."
    />
  );
}
